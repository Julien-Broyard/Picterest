{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="has-text-centered notification is-light is-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}